## 2.0.6

1. Fix showChildTop flicker on layer hide: return GlobalKey to the list one frame before removing Overlay.

## 2.0.5

1. Fix showChildTop flicker on BubblePopupMenuType.layer: claim GlobalKey after OverlayEntry first frame.

